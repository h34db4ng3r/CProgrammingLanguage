#include <stdio.h>
#include <stdlib.h>

int main()
{
    // convention to use capital letters for const
    const int FAV_NUM = 5;
    printf("%d\n", FAV_NUM);
    
    // num = 8;
    //printf("%d\n", num);

    return 0;
}